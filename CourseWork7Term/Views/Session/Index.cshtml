@model IEnumerable<CourseWork7Term.Models.session>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cinema.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.film.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.start_time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ticket_cost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cinema.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.film.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.start_time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ticket_cost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { cinema_id=item.cinema_id, film_id = item.film_id }) |
            @Html.ActionLink("Details", "Details", new { cinema_id=item.cinema_id, film_id = item.film_id }) |
            @Html.ActionLink("Delete", "Delete", new { cinema_id = item.cinema_id, film_id = item.film_id })
        </td>
    </tr>
}

</table>
