@model CourseWork7Term.Models.actor

@{
    ViewBag.Title = "Удаление записи об актере";
}

<h2>Удаление записи об актере</h2>

<h3>Удалить запись?</h3>
<fieldset>
    <legend>actor</legend>

    <div class="display-label">
         Имя
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.name)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Удалить" /> |
        @Html.ActionLink("К списку актеров", "Index")
    </p>
}
